type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
    characters: [Character]
}

type Author {
    id: ID
    firstName: String
    lastName: String
}

type Character {
    id: ID
    firstName: String
    lastName: String
}
